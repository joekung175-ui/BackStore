import { supabase } from './supabase.js';
import { escapeHtml, qs, setLoading, toast } from './utils.js';

if (document.body.dataset.page === 'admin-product-form') {
  const mount = async () => {
    const container = document.querySelector('#admin-content'); if (!container || container.dataset.productFormMounted) return;
    container.dataset.productFormMounted = 'true';
    document.querySelector('.admin-header h1').textContent = 'เพิ่มสินค้า';
    if (!supabase) { container.innerHTML='<div class="alert alert-warning">กรุณาตั้งค่า Supabase ก่อนเพิ่มสินค้า</div>'; return; }
    const { data: categories, error } = await supabase.from('categories').select('id,name').is('deleted_at',null).eq('is_active',true).order('name');
    if (error) { container.innerHTML=`<div class="alert alert-danger">${escapeHtml(error.message)}</div>`; return; }
    container.innerHTML = `<div class="row justify-content-center"><div class="col-xl-9"><form id="product-create-form" class="card border-0 shadow-sm"><div class="card-body p-4 p-md-5"><div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="h4 mb-1">ข้อมูลสินค้า</h2><p class="text-secondary small mb-0">กรอกข้อมูลที่จำเป็นก่อนเปิดขาย</p></div><a href="products.html" class="btn btn-outline-primary btn-sm">กลับรายการสินค้า</a></div><div class="row g-3"><div class="col-md-8"><label class="form-label">ชื่อสินค้า</label><input class="form-control" name="name" required placeholder="เช่น ชาอู่หลงพรีเมียม"></div><div class="col-md-4"><label class="form-label">SKU</label><input class="form-control" name="sku" required placeholder="เช่น TEA-013" style="text-transform:uppercase"></div><div class="col-md-6"><label class="form-label">หมวดหมู่</label><select class="form-select" name="category_id" required><option value="">เลือกหมวดหมู่</option>${categories.map(c=>`<option value="${c.id}">${escapeHtml(c.name)}</option>`).join('')}</select></div><div class="col-md-6"><label class="form-label">ลิงก์รูปภาพ (ไม่บังคับ)</label><input class="form-control" type="url" name="image_url" placeholder="https://..."></div><div class="col-md-4"><label class="form-label">ราคาขาย (บาท)</label><input class="form-control" type="number" name="price" min="0" step="0.01" required></div><div class="col-md-4"><label class="form-label">ต้นทุน (บาท)</label><input class="form-control" type="number" name="cost" min="0" step="0.01" value="0"></div><div class="col-md-4"><label class="form-label">ราคาเดิม (บาท)</label><input class="form-control" type="number" name="compare_at" min="0" step="0.01"></div><div class="col-md-6"><label class="form-label">สต็อกเริ่มต้น</label><input class="form-control" type="number" name="stock" min="0" step="1" value="0" required></div><div class="col-md-6"><label class="form-label">แจ้งเตือนเมื่อเหลือ</label><input class="form-control" type="number" name="low_stock_threshold" min="0" step="1" value="5" required></div><div class="col-12"><label class="form-label">รายละเอียดสินค้า</label><textarea class="form-control" name="description" rows="3" placeholder="บรรยายรสชาติ จุดเด่น หรือวิธีชง"></textarea></div><div class="col-md-6"><div class="form-check"><input class="form-check-input" type="checkbox" id="featured" name="featured"><label class="form-check-label" for="featured">แสดงเป็นสินค้าแนะนำ</label></div></div><div class="col-md-6"><div class="form-check"><input class="form-check-input" type="checkbox" id="best" name="best"><label class="form-check-label" for="best">ติดป้ายสินค้าขายดี</label></div></div></div><div class="d-flex justify-content-end gap-2 mt-4"><a href="products.html" class="btn btn-outline-secondary">ยกเลิก</a><button class="btn btn-primary" type="submit"><i class="bi bi-plus-circle"></i> บันทึกสินค้า</button></div></div></form></div></div>`;
    qs('#product-create-form').addEventListener('submit', async event => { event.preventDefault(); const form=event.currentTarget, button=qs('[type="submit"]',form), raw=Object.fromEntries(new FormData(form)); const price=Math.round(Number(raw.price)*100), cost=Math.round(Number(raw.cost||0)*100), compare=raw.compare_at===''?null:Math.round(Number(raw.compare_at)*100); if (compare !== null && compare < price) { toast('ราคาเดิมต้องมากกว่าหรือเท่ากับราคาขาย','danger'); return; } try { setLoading(button,true); const { data: product, error: productError }=await supabase.from('products').insert({category_id:raw.category_id,name:raw.name.trim(),sku:raw.sku.trim().toUpperCase(),description:raw.description.trim()||null,price_satang:price,cost_satang:cost,compare_at_satang:compare,total_stock:Number(raw.stock),low_stock_threshold:Number(raw.low_stock_threshold),is_featured:form.featured.checked,is_best_seller:form.best.checked}).select('id').single(); if(productError) throw productError; if(raw.image_url.trim()){const {error:imageError}=await supabase.from('product_images').insert({product_id:product.id,url:raw.image_url.trim(),alt_text:raw.name.trim(),storage_path:`external-admin/${product.id}.jpg`,sort_order:0});if(imageError)throw imageError;} toast('เพิ่มสินค้าเรียบร้อยแล้ว'); location.assign('products.html'); } catch(err){toast(err.message,'danger')} finally {setLoading(button,false)} });
  };
  new MutationObserver(() => mount().catch(error=>toast(error.message,'danger'))).observe(document.documentElement,{childList:true,subtree:true}); mount().catch(error=>toast(error.message,'danger'));
}
